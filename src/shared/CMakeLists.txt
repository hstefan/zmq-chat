set(SOURCE
  hello.proto
)

PROTOBUF_GENERATE_CPP(PROTO_SRCS PROTO_HDRS ${SOURCE})
set(PROTO_INCLUDE_DIRS ${CMAKE_CURRENT_BINARY_DIR} PARENT_SCOPE)

add_library(zmqchat STATIC ${PROTO_SRCS} ${PROTO_HDRS})
target_link_libraries(zmqchat ${PROTOBUF_LIBRARIES})
